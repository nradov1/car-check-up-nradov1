package com.infinum.course.car.checkup.service

import com.infinum.course.car.checkup.repository.CarCheckUpRepository

class CarCheckUpSystem(carCheckUpRepository: CarCheckUpRepository) {
    //No use right now
}